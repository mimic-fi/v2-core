{
  "name": "@mimic-fi/v2-core",
  "private": true,
  "version": "0.0.1",
  "license": "GPL-3.0",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test"
  },
  "dependencies": {},
  "devDependencies": {},
  "workspaces": {
    "packages": [
      "packages/helpers",
      "packages/registry",
      "packages/price-oracle",
      "packages/swap-connector",
      "packages/bridge-connector",
      "packages/strategies",
      "packages/smart-vault"
    ]
  }
}
